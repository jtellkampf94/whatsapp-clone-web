mutation Login($options: LoginInput!) {
  login(options: $options) {
    ok
    user {
      id
      email
      username
      updatedAt
      createdAt
    }
    errors {
      field
      message
    }
  }
}
